
<div id="navigation" class="navbar navbar-fixed-top" ng-controller="NavigationCtrl">
    <div class="navbar-inner">
        @*<div class="container">*@
            <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>

            <a class="brand" href="/">Bookkeeper</a>

            <div class="nav-collapse collapse">
                <button class="btn btn-default btn-small pull-right" ng-click="logout()">Logout</button>
                <p class="navbar-text pull-right">{{user.Name}}</p>
            </div>

        @*</div>*@
    </div>

</div>

@*<nav id="navigation" class="navbar navbar-default navbar-fixed-top" role="navigation" ng-controller="NavigationCtrl">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">Bookkeeper</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse ">
        <button class="btn btn-default btn-small navbar-btn navbar-right" ng-click="logout()">Logout</button>
        <p class="navbar-text navbar-right">{{user.Name}}</p>
    </div>
    <!-- /.navbar-collapse -->
</nav>*@
